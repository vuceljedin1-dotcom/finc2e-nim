# NGC Personal API Key (starts with nvapi-...)
NGC_API_KEY=

# GPU selection
NVIDIA_VISIBLE_DEVICES=all

# Optional: if you want to pin a specific GPU architecture behavior
# CUDA_VISIBLE_DEVICES=0

# Start
docker compose up -d
docker ps
docker logs -f finc2e-nim

# Health
curl -sS http://localhost:8000/health || curl -sS http://localhost:8000/v1/health
