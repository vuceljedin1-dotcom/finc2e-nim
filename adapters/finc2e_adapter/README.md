# FinC2E Adapter (LOCAL ONLY)

This folder is mounted into the container at runtime.

Do NOT commit weights or secrets to GitHub.

Expected local files:
- adapter_model.safetensors
- adapter_config.json
- config.json (optional)

